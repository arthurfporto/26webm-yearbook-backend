info:
  name: Atualizar aluno sem token
  type: http
  seq: 5

http:
  method: PUT
  url: "{{baseUrl}}/alunos/1"
  body:
    type: json
    data: |-
      {
        "cidade": "Montes Claros",
        "frase": "Frase atualizada"
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar 401", function() {
          expect(res.getStatus()).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
