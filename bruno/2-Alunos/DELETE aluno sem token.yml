info:
  name: DELETE aluno sem token
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/alunos/{{alunoCriadoId}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar 401", function() {
          expect(res.getStatus()).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
