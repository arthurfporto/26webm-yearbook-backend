meta {
  name: DELETE aluno com token (ADMIN)
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/alunos/{{alunoCriadoId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("deve retornar 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
