meta {
  name: Criar mensagem sem token
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/mensagens
  body: json
  auth: none
}

body:json {
  {
    "texto": "Mensagem sem token teste"
  }
}

tests {
  test("deve retornar 401", function() {
    expect(res.getStatus()).to.equal(401);
  });
}
