info:
  name: Deletar mensagem (logado/ADMIN)
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/mensagens/{{mensagemCriadaId}}"
  auth:
    type: bearer
    token: "{{tokenAdmin}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar status 204", function() {
          expect(res.getStatus()).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
